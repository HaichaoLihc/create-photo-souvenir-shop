// Shared by keyboard and touch movement. The current pace is 70% of the original.
export const SETTINGS = Object.freeze({
  walkingSpeed: 1.65 * 0.7,
  sprintSpeed: 2.5 * 0.7,
  maxPixelRatio: 1.7,
});
